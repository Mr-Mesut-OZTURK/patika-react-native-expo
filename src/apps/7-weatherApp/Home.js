import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import WeatherAppLayout from '@src/src/layouts/weatherAppLayout/WeatherAppLayout'

const Home = () => {
    return (
        <WeatherAppLayout>
            <Text>Home</Text>
        </WeatherAppLayout>
    )
}

export default Home

const styles = StyleSheet.create({})